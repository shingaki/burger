<div class="container"><div class="text-center"><img src="assets/img/burger_one.png"></div>
    <br>
    <div class="text-center"><h1>Eat-Dat-Burger!</h1></div>
    <div class="row text-center">
        <div class="col-md-6">

<h4>Burgers ready to be devoured!</h4>
<ul>
    {{#each burgers}}
        {{#unless devoured}}
            {{> burgers/burger-block devoured=false}}
        {{/unless}}
    {{/each}}
</ul></div>

<div class="col-md-6">
<h4>Burgers already devoured!</h4>

<ul>
    {{#each burgers}}
        {{#if devoured}}
            {{> burgers/burger-block devoured=true}}
        {{/if}}
    {{/each}}
</ul>
    </div></div>
<br><br>
<h1 class="text-center">Add Your Burger</h1>
<form class="create-form">
    <div class="form-group">
        <label for="newburger">Burger Name:</label>
        <input type="text" id="newburger" name="name">
    </div>


    <button type="submit">Add Your Burger</button>
</form>
</div>